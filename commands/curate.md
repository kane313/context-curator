---
description: 沉淀会话知识到项目上下文资产（CLAUDE.md / memory / docs / skills）
---

用 context-curator skill 沉淀上下文资产。

参数：$ARGUMENTS

- 无参数或「结算」→ 结算模式，处理队列里 pending 的会话
- 「全量」「扫一遍」→ 全量模式，扫描该项目全部历史会话
- 「当下」「这次」→ 当下模式，只沉淀当前会话

严格遵守 skill 的三条铁律：没有确认不写、没有证据不提、拒绝过的不再提。
